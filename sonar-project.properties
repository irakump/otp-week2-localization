sonar.projectKey=otp-week2-localization
sonar.projectName=otp-week2-localization

sonar.projectVersion=1.0
sonar.sources=src
sonar.java.binaries=target/classes
sonar.host.url=http://localhost:9000

# run with (after Sonar Scanner is turned on from terminal, use StartSonar.bat):
# mvn clean verify sonar:sonar "-Dsonar.projectKey=otp-week2-localization" "-Dsonar.login=$env:SONAR_TOKEN" "-Dsonar.host.url=http://localhost:9000"
